trigger:
  branches:
    include:
    - release
    - develop
  paths:
    exclude:
    - README.md
    - .gitignore
  
jobs:
- job: Build
  pool:
    vmImage: 'ubuntu-latest'
  workspace:
    clean: all
  steps:
  - checkout: self
    clean: true
    fetchDepth: 1

  - bash: docker image build . -t thr-step-ca:$(Build.BuildNumber)
  - bash: docker image save thr-step-ca:$(Build.BuildNumber) -o $(Build.ArtifactStagingDirectory)/thr-step-ca.tar

  - task: PublishBuildArtifacts@1
    inputs:
      pathToPublish: $(Build.ArtifactStagingDirectory)/thr-step-ca.tar
      artifactType: container
      artifactName: step-ca
